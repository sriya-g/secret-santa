# Database connection string
# For development with PostgreSQL:
DATABASE_URL="postgresql://secretsanta:SecretSanta2024DBPass@localhost:5432/secretsanta?schema=public"

# For Docker deployment (use docker-compose.yml instead)
# DATABASE_URL is set automatically in docker-compose.yml

# Optional: Database password for Docker deployment
# DB_PASSWORD=your-secure-password-here

# Email Configuration for Magic Links
# SMTP settings (Gmail, Outlook, custom SMTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM="Secret Santa <noreply@yourdomain.com>"

# Magic link settings
MAGIC_LINK_SECRET=your-super-secret-key-for-signing-tokens
MAGIC_LINK_EXPIRES_MINUTES=15
NEXTAUTH_URL=https://santa.wagnerway.co.za
